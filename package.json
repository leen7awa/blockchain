{
    "scripts": {
        "start": "concurrently \"npm run dev --prefix front\" \"npm start --prefix back\"",
        "build": "react-scripts build"
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "dependencies": {
        "bcrypt": "^5.1.1"
    }
}
